import 'package:flutter/material.dart';

class AmbulancesPage extends StatelessWidget {
  const AmbulancesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [Text("Ambulances")],
    );
  }
}
